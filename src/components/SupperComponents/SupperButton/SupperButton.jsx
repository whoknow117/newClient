import React from 'react';
import classes from './SupperButton.module.scss';


const SupperButton = ({changeItem,}) => {
    return (
        <div>

        </div>
    );
};

export default SupperButton;